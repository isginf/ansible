---
remote_ip:
  - 1.2.3.4 # git.example.com

commands:
  - git reset --hard
  - git clean -f
  - git pull
  - git submodule update -f --init --recursive --remote

repos:
  inventory: .
  bootstrap: .
